{
  "name": "planner",
  "version": "1.0.0",
  "description": "Guest planner and RSVP management system",
  "private": true,
  "workspaces": [
    "client",
    "api",
    "server"
  ],
  "scripts": {
    "dev": "concurrently \"cd client && npm run dev\" \"cd server && npm start\"",
    "dev:watch": "concurrently \"cd client && npm run dev\" \"cd server && npm run dev\"",
    "build": "cd client && npm run build",
    "preview": "cd client && npm run preview"
  },
  "dependencies": {
    "@libsql/client": "^0.5.6",
    "cors": "^2.8.6",
    "express": "^5.2.1",
    "firebase": "^12.13.0"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
